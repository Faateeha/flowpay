import React from 'react'

const HeaderBox = ({type = "title", tittle, subtext, user} : HeaderBoxProps) => {
  return (
    <div>
      HeaderBox
    </div>
  )
}

export default HeaderBox

